<% content_for :title do %>Sign In to BulldogClip - Manage Your Expenses Online<% end %>
<% content_for :description do %>Sign In to BulldogClip. Organise your receipts. Track costs with graphs & create invoices. No more complex spreadsheets. Save time & money on bookkeeping.<% end %>

<div class="container">
  <div class="panel panel-default col-md-4 col-md-offset-4">
    <div class="panel-body">
      <div class="authform">
        <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
          <h1>Sign in</h1>
            <%= render "devise/shared/error_messages", resource: resource %>
          <div class="form-group">
            <%- if devise_mapping.registerable? %>
            <%= link_to 'Sign up', plans_path, class: 'pull-right', data: {toggle: "tooltip"}, title: "Sign up for a BulldogClip account", tabindex: 0 %>
            <% end -%>
            <%= f.label :email %>
            <%= f.email_field :email, :autofocus => true, class: 'form-control', type: "email", tabindex: 1 %>

          </div>
          <div class="form-group">
            <%- if devise_mapping.recoverable? %>
            <%= link_to "Forgot password?", new_password_path(resource_name), class: 'pull-right', data: {toggle: "tooltip"}, title: "Reset your password", tabindex: 0%>
            <% end -%>
            <%= f.label :password %>
            <%= f.password_field :password, class: 'form-control', tabindex: 2 %>

          </div>
            <%= f.submit 'Sign in', :class => 'create-btn std-btn pull-right', data: {toggle: "tooltip"}, title: "Sign in to your BulldogClip account", tabindex: 3 %>
            <% if devise_mapping.rememberable? -%>
              <div class="checkbox">
                <%= f.check_box :remember_me %> <%= f.label :remember_me %>
              </div>
            <% end -%>
        <% end %>
      </div>
    </div>
  </div>
</div>