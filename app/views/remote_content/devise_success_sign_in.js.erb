$('.modal').modal('hide')

if($('#flash').length > 0) {
  $('#flash').remove();
  $('main').prepend("<%= escape_javascript raw(flash_normal) %>");
  // window.location.replace("/welcome");
}
else {
  $('.modal').modal('hide')
  $('main').prepend("<%= escape_javascript raw(flash_normal) %>");
  // window.location.replace("/welcome");
}

if($("body[data-controller|='pages']").length > 0){
  window.location.replace("/welcome");
  //otherwise stay on the page we are on.
}